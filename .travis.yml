---
sudo: required
language: ruby
services:
  - docker
before_install:
  - docker build -t franklinkim/ansible-trusty .
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push franklinkim/ansible-trusty
script:
  - rake test
